
# PDF Extraction 
PyMuPDF              # PDF text extraction (import as 'fitz')

# Text Processing & Chunking 
langchain             # RecursiveCharacterTextSplitter
langchain-community   # LangChain community integrations
langchain-text-splitters

# Embeddings & Semantic Search 
sentence-transformers # all-MiniLM-L6-v2 + cross-encoder
faiss-cpu             # FAISS IndexFlatIP (CPU build)
numpy                # Vector arrays used by FAISS

# Keyword Search 
rank-bm25             # BM25Okapi keyword index

# LLM (Groq / Llama 3) 
groq                  # Groq API client

# Web UI 
streamlit          # 3-tab web application

# Auth & Storage 
bcrypt                # Password hashing for user store
# sqlite3 is part of the Python standard library — no install needed

# Environment Variables 
python-dotenv         # Loads .env file (GROQ_API_KEY, etc.)

# Utilities 
tqdm                 # Progress bars during ingestion